# WFCSystem v1 — log técnico

## 2026-09-11 12:29 (America/Sao_Paulo)

- Repositório `developrhs/wfcimoveis_wfcsystem_v1` clonado e revisado.
- Os arquivos `README_wfcsystem.md` e `wfcsystem_log.txt` não existiam no clone; este log foi criado para manter o histórico solicitado.
- O cliente desktop Java utiliza corretamente a API PHP com sessão HTTP e **não deve** armazenar ou usar credenciais MySQL diretamente.
- Verificação pública: `GET https://wfcimoveis.com/sistema/api/v1/health` respondeu HTTP `503` com `{"ok":false,"error":"Serviço indisponível.","code":"DB_CONNECTION_FAILED"}`.
- Conclusão: o login não pode funcionar enquanto a API PHP não conseguir abrir a conexão MySQL.
- Alteração da versão 0.1.1: antes de enviar credenciais, o cliente consulta `/health` e exibe uma mensagem específica para `DB_CONNECTION_FAILED`, evitando a falsa impressão de que o problema está na tela de login.

## Correção necessária no HostGator

No backend, conferir `deploy/wfc_sistema/api/config/local.php` (fora do Git) ou as variáveis equivalentes: `WFC_DB_HOST`, `WFC_DB_NAME`, `WFC_DB_USER` e `WFC_DB_PASS`. Para uma API PHP hospedada na mesma conta, o host normalmente deve ser o host MySQL indicado no cPanel, frequentemente `localhost`; o nome do banco e do usuário devem incluir o prefixo da conta cPanel. A porta padrão é `3306`.

Se a conexão for feita externamente pelo MySQL Workbench, o cPanel precisa liberar o IP público atual em **Remote MySQL** e o servidor/porta/usuário devem ser os informados pelo HostGator. Essa regra de allowlist é aplicável ao Workbench, mas não substitui a configuração da API PHP. O desktop continua acessando apenas HTTPS.

Após corrigir o backend, validar novamente `/health` esperando HTTP 200 e então testar `/auth/login` no aplicativo. Nenhuma senha de cPanel, FTP ou banco deve ser commitada no repositório.

## 2026-09-11 13:28 (America/Sao_Paulo)

- Revisado o repositório desktop correto `developrhs/wfcimoveis_wfcsystem_v1`; o repositório web `developrhs/wfcimoveis_com` permanece separado.
- Confirmado que o desktop Java acessa somente a API HTTPS e não deve receber usuário, senha ou porta do MySQL diretamente.
- Atualizada a versão Maven de `0.1.1` para `0.1.2`; o artefato esperado passa a ser `wfcsystem-v1-0.1.2.jar`.
- Atualizado o README para distinguir `DB_CONFIG_MISSING` de `DB_CONNECTION_FAILED` e registrar a regra HostGator: o PHP usa o host indicado no cPanel (frequentemente `localhost`), banco e usuário com prefixo da conta, senha do usuário MySQL e porta 3306.
- A regra de liberação de IP em Remote MySQL é necessária para o Workbench externo, mas não substitui a configuração da API PHP; o desktop continua usando HTTPS.
- Nenhuma credencial recebida foi gravada em arquivo, commit ou artefato.
- Próxima validação: executar o build Maven e confirmar o endpoint `/health` com HTTP 200 após a correção do `local.php`/variáveis no HostGator.

## 2026-09-11 13:29 (America/Sao_Paulo)

- O primeiro build falhou porque o ambiente tinha apenas o JRE; o JDK 21 foi instalado e a compilação Maven foi concluída com sucesso usando `maven.compiler.release=17`.
- Artefato gerado: `target/wfcsystem-v1-0.1.2.jar`.
- SHA-256 do JAR: `10ff17ee44b46a7a251fbf74d7c9006eff555230c41666d11ab8793ae0e3c36e`.
- O JAR contém o manifesto com `com.wfcimoveis.wfcsystem.Main` como classe principal.
- Não foi feita tentativa de login usando a senha de cPanel/FTP fornecida; essas credenciais não são equivalentes à senha do usuário MySQL e não devem ser embutidas no desktop.

## 2026-09-11 13:30 (America/Sao_Paulo)

- Nova verificação segura de `GET https://wfcimoveis.com/sistema/api/v1/health`: HTTP `503`, sem autenticação e sem envio de senha; o backend ainda não comprova conexão MySQL funcional.
- `mvn test` concluído com sucesso e o manifesto do JAR confirma `Main-Class: com.wfcimoveis.wfcsystem.Main`.

## 2026-09-11 15:43 (America/Sao_Paulo)

- Criada a página `maintenance.html`/`index.html` de manutenção, com previsão pública até 30/09/2026 e atendimento via WhatsApp oficial `+55 62 99511-1648`.
- O contato foi reaproveitado do código existente do site; nenhuma credencial foi incluída nos arquivos.
- A inspeção do cPanel confirmou que `public_html` contém `wfc-asset-patch.zip`, enquanto `cwcimoveis.com` e `wfc_sistema` aparecem vazios no Gerenciador de Arquivos; portanto, a troca do `index.html` não foi feita em diretório não confirmado.
- O domínio continua respondendo uma aplicação React/Vite; a publicação definitiva da manutenção requer confirmar o document root/Application Manager efetivo.

## 2026-09-11 15:58 (America/Sao_Paulo)

- Iniciada a versão 0.2.0 do WFCSystem v1 com banco local SQLite em `~/.wfcsystem/wfcsystem.db`.
- Adicionadas as tabelas locais `app_meta`, `local_records` e `sync_queue`, com fila para alterações offline.
- Adicionado cofre local `~/.wfcsystem/credentials.vault`, protegido por PBKDF2-HMAC-SHA256 e AES-256-GCM; a senha-mestra não é persistida.
- Adicionada tela de configuração para cPanel, FTP, phpMyAdmin e MySQL administrativo. Os valores ficam fora do JAR, fora do Git e não são escritos em logs.
- Adicionado modo offline, botão de sincronização e tentativa automática a cada 60 segundos.
- Implementados os clientes HTTP para `POST /sync/push` e `GET /sync/pull?since=...`; enquanto a API não disponibilizar esses endpoints, as alterações permanecem preservadas na fila SQLite.
- Build `mvn clean test package` aprovado com JDK 17; o artefato recomendado é o JAR standalone com o driver SQLite incluído.
- Teste de fumaça confirmou a criação de `wfcsystem.db` em um diretório de usuário temporário.

## 2026-09-11 16:33 (America/Sao_Paulo)

- Corrigido o login do modo offline: não existe mais entrada direta sem autenticação.
- Adicionado cadastro local inicial dos quatro usuários fornecidos, com dados de perfil e contato no SQLite.
- As senhas iniciais não foram armazenadas em texto aberto; foram convertidas para hashes PBKDF2-HMAC-SHA256 com salt individual.
- Usuário desconhecido, senha incorreta, username vazio ou conta inativa são rejeitados antes de abrir a tela principal.
- Em indisponibilidade da API, o login online tenta fallback local somente após validar o cadastro e a senha no banco local.
- O primeiro username aparece pré-preenchido por conveniência; a senha permanece vazia e precisa ser digitada.
- Teste de fumaça confirmou login válido aceito e senha inválida/usuário inexistente rejeitados.

## 2026-09-11 20:33 (America/Sao_Paulo)

- Iniciada a consolidação da base da versão 0.3.0 no repositório versionado `developrhs/wfcimoveis_wfcsystem_v1`, usando o pacote enviado apenas como referência de funcionalidades.
- Adicionadas ao SQLite as tabelas e operações locais para gestão de imóveis, clientes, agentes, vendas e usuários; a aba de usuários permite alterar perfil (`Administrador`, `Corretor` ou `Atendimento`) e status ativo/inativo.
- Adicionada a tabela `image_queue`, com status pendente/enviada, caminho temporário, entidade relacionada e mensagem de erro.
- Implementada a criação automática de `<java.io.tmpdir>/wfcsystem-images` e subpastas para imagens. O arquivo temporário só é removido após confirmação do upload FTP.
- Implementado serviço de FTP em modo passivo e binário usando Apache Commons Net; nenhuma credencial foi gravada no código, no Git ou neste log.
- Integrado painel Swing com tabelas para os módulos de gestão e seleção de imagens no cadastro de imóveis.
- Próxima validação: compilar com Maven, executar testes de schema/fila, revisar o JAR standalone e só depois realizar teste FTP não destrutivo com credenciais carregadas pelo cofre local.

## 2026-09-11 20:35 (America/Sao_Paulo)

- Instalado o JDK 21, pois o ambiente possuía apenas o runtime Java; `mvn clean test package` foi concluído com sucesso.
- Gerados `target/wfcsystem-v1-0.3.0.jar` e `target/wfcsystem-v1-0.3.0-standalone.jar`.
- O manifesto do standalone confirma `Main-Class: com.wfcimoveis.wfcsystem.Main`; o pacote inclui `AppDatabase`, `ManagementPanel`, `ImageStaging`, SQLite e `org.apache.commons.net.ftp.FTPClient`.
- SHA-256 do JAR comum 0.3.0: `7f4aee653a0e6437c161874084c75c4e237b6258ab57d51998fa9723ecb9f3b6`.
- SHA-256 do JAR standalone 0.3.0: `2b9cf11c179945c6bdc2419bf5b3bfad98f7dce27ca32e16e1be67a6c2a0eebc`.
- O exemplo `config.properties.example` foi alinhado ao host FTP informado `162.241.2.254`; usuário e senha permanecem vazios e nenhuma senha foi gravada nos arquivos versionados.
- A compilação foi validada; ainda falta teste de execução gráfica e teste FTP não destrutivo com credenciais carregadas pelo cofre local.

## 2026-09-12 08:44 (America/Sao_Paulo)

- Alinhada a arquitetura oficial: `https://wfcimoveis.com/` é o site público para clientes; `https://wfcimoveis.com/sistema/` é o painel web permanente; `WfcSystem` é o aplicativo Java local para operação offline e sincronização posterior.
- Confirmado que o painel deve ser instalado exclusivamente em `/home3/cwcimo17/public_html/wfc_sistema/`; a raiz pública existente não deve ser limpa, substituída ou usada como staging.
- Confirmado que o desktop usa a API HTTPS em `https://wfcimoveis.com/sistema/api/v1`, mantém registros no SQLite quando offline e envia imagens pendentes via FTP quando a conexão retorna.
- Encerrado o servidor temporário de distribuição do pacote; a publicação permanente depende da instalação do pacote no cPanel/HostGator e da configuração protegida do banco PHP.
